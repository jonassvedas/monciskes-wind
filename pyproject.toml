[project]
name = "monciskes-wind"
version = "0.1.0"
description = "Monciškės kiteboarding wind notifications"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "Pillow",
    "pytesseract",
    "pytz",
    "requests",
    "suntime",
]
